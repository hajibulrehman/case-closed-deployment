# Backend API origin — only needed when frontend and backend are on different domains.
# Example: VITE_API_URL=https://case-closed-backend.onrender.com
# Leave blank if the backend serves the frontend from the same origin.
VITE_API_URL=
